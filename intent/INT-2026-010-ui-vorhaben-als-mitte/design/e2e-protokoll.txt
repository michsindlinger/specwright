# E2E-Protokoll INT-2026-010 Stufe 1 — 2026-09-16

Umgebung: Branch-Backend auf Port 3111 (Runtime /tmp/sw-3111, eigener tmux-Socket), Scratch-Projekt /tmp/scratch-int010 (git init, Specwright-Rumpf, intent-Workflow und -Vorlage kopiert, CLAUDE.md „höchstens zwei Rückfragen, dann Ordner anlegen") mit zwei Vorhaben: INT-2026-001 (Kopie von INT-2026-010, spec.md `Status: entwurf`, vollständige Leser-Marker) und INT-2026-002 (Kopie von INT-2026-008, unmarkiert). Claude Code 2.1.273, Chrome headless über das globale Playwright 1.57 (Mac 1440×900, Handy 390×844 isMobile/hasTouch), zweites Gerät als WebSocket-Client (workspace:open-project, vorhaben:start-step, cloud-terminal:input). Skript e2e-010.mjs im Scratchpad der Bausitzung (nicht im Repo). Drei Läufe: Lauf 1 (15:30–15:37 UTC) Schritte 1–8 und Handy bis zur Vorhaben-Seite, Lauf „rest" (15:38–15:40) Ordner-Claim und Handy, Lauf „handy" (15:41) letzter Handy-Punkt nach Skriptkorrektur. Ergebnis: alle Prüfpunkte grün (25 Checks), Details unten.

## Schritte und Ergebnis

1. UI öffnen → Liste (Screenshot ist-01-liste-mac.png, ist-01-liste-handy.png). Kopfzeile Mac: genau 2 Bedienelemente (Glocke, Projekt-Symbol), keine Seitenleiste, keine Projekt-Reiter, keine Git-Leiste, keine Modellwahl, kein Versions-Label (FA-07, EK-02). Liste: Titel „Vorhaben", Chips „Alle · Scratch 010", Gruppen „Wartet auf dich · n" und „Läuft · n" (AN-S01), feste Leiste „Neue Absicht" unten, keine Kopfzeile „n Projekte · m warten" (FA-02, AN-S02).
2. Sitzung per WS gestartet (vorhaben:start-step, spec INT-2026-001, Haiku; Trust-Dialog des frischen Scratch-Ordners über tmux bestätigt). Cmd+D öffnet die Terminal-Sidebar, die Sitzung ist der aktive Tab; Cmd+D schließt sie wieder (FA-08). Glocke ohne Zahl.
3. Stop-Ereignis der zuletzt aktiven Sitzung bei geschlossener Sidebar (Haiku hat die Spec geschrieben und „freigegeben" gesetzt): Kopfzeile zeigt „1" (FA-04; Review E2 — vorher wäre diese Sitzung stumm geblieben). Messung EK-04/FA-05: Empfang des cloud-terminal:agent-event-Frames im Browser → Zahl im DOM (Polling 25 ms): 2 ms; Zeitstempel des Hooks → Zahl: 165 ms. Ziel < 2 s erfüllt. Screenshot ist-04-glocke-zahl.png.
4. Glocke-Klick → Liste „fertig · Scratch 010 · spec INT-2026-001 · gerade eben · Spec freigegeben …" (ist-04-glocke-offen.png) → Klick auf den Eintrag → #/vorhaben/…/INT-2026-001, Eintrag entfernt (Zahl weg), Vorhaben-Seite mit Gespräch rechts und Eingabefeld (FA-06). EK-01: 2 Klicks bis zur Seite, die Antwort ist die 3. Aktion (Eingabe). Screenshot ist-04-vorhaben-mac.png.
5. Projekt-Symbol → #/projekt: sieben Abschnitte (projekt, docs, git, dateien, start, einstellungen, vorlagen), Git-Leiste „main · Pull · Push · Commit · Commit & Push", Projektliste mit „aktiv"-Marke, „Projekt hinzufügen", Erste Schritte mit Update-Hinweis und Validierung (Scratch: „Installation unvollständig"), Auslastung nicht sichtbar (0 laufende Sitzungen am Gate) (FA-17). Screenshot ist-02-projekt-mac.png, ist-02-projekt-handy.png. Einstellungen aufgeklappt, alle fünf Reiter (Models, General, Git Integration, Appearance, Setup) angeklickt: Reiter wechseln lokal, Route bleibt #/projekt, kein Voice-Reiter (FA-19). Screenshot ist-02-projekt-einstellungen.png.
6. Alte Adressen: #/chat → Liste (Titel „Vorhaben"); #/settings/models → Projekt-Seite (Titel „Projekt"); #/nirgendwo → 404 mit Link #/vorhaben (FA-18).
7. „Neue Absicht" in der Leiste → #/neu/<pid>, Seite „Neue Absicht" mit dem verschobenen Start-Block (Zwischenstand, ist-03-neu-zwischenstand.png). „Absicht beginnen" → /intent-Sitzung (Vorauswahl aus lastModel/stepDefault = Opus 5, nicht Haiku), Gespräch erscheint rechts neben dem Block (ist-03-neu-gespraech.png; Review F8). Erste Frage beantwortet über cloud-terminal:input (Text und Enter getrennt — Enter im selben Chunk verschluckt das TUI), zweite Frage von Hand über tmux beantwortet (Skript sah den zweiten Stop nicht, weil er vor dem Neustart des Skripts lag). Ordner intent/INT-2026-003-projekt-filter angelegt → UI navigiert von #/neu auf #/vorhaben/…/INT-2026-003 (AK-09, Review F8), 95 s ab Skriptstart „rest". Screenshot ist-03-neu-claim.png.
8. Handy 390 px: Kopfzeile mit 3 Symbolen (Glocke, Projekt, Terminal), kein alter Handy-Rahmen (Bottom-Nav, Top-Bar, Drawer, Pill), keine Querscroll (FA-07, FA-20). Terminal-Symbol öffnet die Sidebar (FA-09; ist-05-terminal-handy.png); die Sidebar liegt am Handy als Vollbild über der Kopfzeile und schließt über ihren eigenen Knopf (sidebar-close) — so wie bisher. Projekt-Seite: 7 Abschnitte, keine Querscroll. Vorhaben-Seite ohne Gespräch (NZ-01), Leser sichtbar (ist-04-vorhaben-handy.png). Handy-Zweig des Terminal-Sprungs (Review F11): open-terminal-session setzt die Sitzung als aktiven Tab und öffnet die Sidebar, kein Solo/Vollbild (ist-05-terminal-sitzung-handy.png). Hinweis: der Knopf „Im Terminal öffnen" auf der Vorhaben-Seite kommt mit der Aktionsleiste der Stufe 2 (Plan §3); in Stufe 1 sendet die Sende-Leiste das Ereignis nur im Dialog-Zustand („Zum Terminal ›"), das Skript hat das Ereignis daher direkt ausgelöst.
9. Backend-Log 3111: keine „agent-event rejected"-Zeilen.

## Screenshots neben dem Mock

- skizze-01.jpg (Liste, Rahmen) ↔ ist-01-liste-mac.png, ist-01-liste-handy.png: Kopfzeile links Titel, rechts Glocke/Projekt(/Terminal); Chips, zwei Gruppen, Leiste unten. Abweichung: die Leiste steht seit dem letzten Build fest am unteren Fensterrand (position: fixed), im Screenshot des Laufs noch direkt unter der Liste (sticky).
- skizze-01.jpg (Neue Absicht) ↔ ist-03-neu-zwischenstand.png: Zwischenstand mit Start-Block statt Textfeld/Modell/Starten — Stufe 2.
- skizze-01.jpg (Vorhaben-Seite) ↔ ist-04-vorhaben-mac.png: Gespräch rechts, Dokument links, Sende-Leiste über die ganze Breite. Phasen-Chips und Aktionsleiste — Stufe 2; Reiter intent.md/spec.md bleiben bis dahin. **Nachtrag 16.09. (Split-Fix):** die Seitenzuordnung war hier falsch als Treffer gewertet — Skizze 3 und FA-12 haben das Gespräch links und das Dokument rechts, 50/50; korrigiert mit dem Fix nach Stufe 2 (unten).
- Projekt-Seite und Handy ohne Skizze: Beschreibung Plan §3 gilt; ist-02-projekt-mac.png, ist-02-projekt-handy.png.

## Messwerte

- FA-05/EK-04 Glocke: 2 ms (Frame → DOM), 165 ms (Hook-Zeitstempel → DOM).
- EK-01: 2 Klicks (Glocke, Eintrag) bis zur Seite mit Eingabefeld.
- Ordner-Claim (Opus 5, zwei Antworten): 95 s ab Start des Lauf-„rest" bis zur Navigation.

# E2E-Protokoll INT-2026-010 Stufe 2 — 2026-09-16

Umgebung: Branch-Backend `feat/INT-2026-010-s2` auf Port 3111 (Runtime /tmp/sw-3111 aus Stufe 1 weiterverwendet, tmux-Server neu), dasselbe Scratch-Projekt /tmp/scratch-int010 wie in Stufe 1 (dazu `specwright/workflows/core/spec.md` kopiert; INT-2026-001/spec.md vor jedem Freigabe-Lauf auf `Status: entwurf` gesetzt). Claude Code 2.1.273, Chrome headless über das globale Playwright 1.57 (Mac 1440×900, Handy 390×844 isMobile/hasTouch), zweites Gerät = zweiter Browser-Kontext. Skript e2e-010-s2.mjs im Scratchpad der Bausitzung (nicht im Repo), Phasen chips · neu · claimed · freigabe (2 Läufe) · handy · aktionen. Läufe 19:51–20:05 (Europe/Berlin). Ergebnis: 33 von 37 Prüfpunkten grün; die vier roten sind unten erklärt (zwei Testdaten, zwei Zeitfenster), kein offener UI-Befund.

## Schritte und Ergebnis

1. Liste → „Neue Absicht" in der festen Leiste → #/neu/<pid>. Seite zeigt genau drei Elemente: Textfeld mit Platzhalter „Was stört, wen, seit wann?", Modellwahl (vorbelegt mit dem Schritt-Standard für intent = Opus), Knopf „Starten" (gesperrt, solange das Feld leer ist) (FA-10). Screenshot ist-03-neu-mac.png.
2. Modell Haiku gewählt, zwei Sätze eingetippt, „Starten": Backend antwortet `vorhaben:step-started`, `pendingIntents[0].session.firstInputPending = true`; die Seite zeigt statt des Formulars die Karte „Absicht-Sitzung „intent" läuft — Vorhaben entsteht …" mit dem Hinweis „Dein Text wird nach der ersten Frage übergeben", rechts das Gespräch (ist-03-neu-gestartet.png).
3. Erster Stop der Sitzung 10,7 s nach dem Start: Protokolleintrag `freitext` mit dem Text (`gesendet`, kurz darauf `angenommen`), `firstInputPending` weg, der Text steht als „Du"-Beitrag nach Claudes erster Frage im Gespräch (AK-09, FA-11; ist-03-neu-beitrag.png). Die Glocke zeigt „1" (Stop bei geschlossener Sidebar, Stufe 1).
4. Zweite Antwort über das Gesprächs-Eingabefeld der UI („Einreihen", weil Haiku noch arbeitete) → Ordner intent/INT-2026-004-erledigte-vorhaben-ausblenden angelegt → UI navigiert von #/neu auf #/vorhaben/…/INT-2026-004, 45 s nach dem Start (AK-09). Beide Protokolleinträge tragen danach die Kennung INT-2026-004.
5. Rot (Testdaten): Haiku schrieb intent.md ohne YAML-Kopf (Scratch-CLAUDE.md „kurz halten"), der Reader liefert Phase `unbekannt` → Chips ohne erreichte Phase, keine Aktionsleiste. Nach Ergänzen des Kopfs von Hand (status entwurf) zeigt die Seite `intent` als erreichte und gewählte Phase, „Freigeben" frei (Sitzung wartet), keinen nächsten Schritt in Phase absicht (2/2, Phase claimed; ist-04-vorhaben-neu-mac.png). Agentenverhalten im Scratch, kein UI-Befund.
6. INT-2026-001 (spec.md entwurf, Sitzung aus Stufe 1 beendet): Kopf mit Kennung groß, Titel, Chips `intent · spec (erreicht, gewählt) · plan (gestrichelt, kein Dokument) · build (gestrichelt)` (FA-12; ist-04-vorhaben-mac.png). Aktionsleiste unter dem Dokument: „Spec schreiben" mit Modell- und Arbeitskopie-Wahl (frei, keine Sitzung) und „Freigeben" (ist-04-aktionsleiste-mac.png).
7. Chip-Klick `intent` auf Gerät 1 → Gerät 2 zeigt denselben Chip nach 4 ms (`vorhaben:ansicht.set` → Broadcast; FA-12, AR-05; ist-04-chips-sync-geraet2.png). Chip `plan` ohne Datei → „Kein Dokument in dieser Phase", Aktionsleiste bleibt (ist-04-kein-dokument.png). Nach Neuladen von Gerät 2 steht die Wahl noch (Backend-Zustand). Projekt-Chip der Liste auf Gerät 1 gewählt → Gerät 2 zeigt „Scratch 010" gewählt (FA-03).
8. „Freigeben" ohne Sitzung (Lauf 1, 19:57): Dialog „Freigabe: spec.md (Stand 2026-09-16 19:51) — startet /specwright:spec INT-2026-001 mit Modell haiku in main und übergibt die Freigabe als erste Eingabe" (Review E11; ist-05-freigabe-dialog.png). „Sitzung starten und freigeben" → `vorhaben:start-step` ohne Modell, Backend löst lastModel = haiku; Zeile `sessionBusy`, „Spec schreiben" gesperrt (ist-05-freigabe-gestartet.png). Rot in Lauf 1: „Freigeben" blieb direkt nach dem Start frei (Sitzung noch `unknown`, erste Eingabe unterwegs) → behoben: gesperrt, solange `session.firstInputPending` (Hinweis „Sitzung startet — die erste Eingabe wird nach der ersten Frage übergeben"), Unit-Test ergänzt. Erster Stop nach 108,6 s (Haiku schrieb die Spec in einem Zug): Freigabe zugestellt, `angenommen`, als „Du"-Beitrag im Gespräch (ist-05-freigabe-beitrag.png — die zweite rote Prüfung war ein Skriptfehler: `startsWith` auf einen Text mit Kopfzeile „Du 19:58"). spec.md `Status: freigegeben` nach 139 s, Zeile: Phase plan, kein `freigabeDoc` mehr (FA-22, AN-S06; ist-05-freigabe-danach.png).
9. „Freigeben" ohne Sitzung (Lauf 2 nach dem Fix, 20:00): beide Knöpfe direkt nach dem Start gesperrt („Spec schreiben" per sessionBusy, „Freigeben" per firstInputPending) — grün. Haikus erster Turn dauerte diesmal länger als das 120-s-Fenster des Skripts (Status freigegeben nach 153 s); die Zustellung folgte beim ersten Stop 178 s nach dem Start und wurde per WS-Snapshot bestätigt (Protokolleintrag `angenommen`). Zwei rote Prüfpunkte = Zeitfenster, kein Befund.
10. Handy 390 px: Seite neu mit Formular, keine Querscroll (ist-03-neu-handy.png). Vorhaben-Seite: Chips oben, kein Gespräch (NZ-01), Aktionsleiste mit gesperrtem „Plan erstellen" (Sitzung wartet) und „Im Terminal öffnen ↗" bei lebender Sitzung; der Knopf öffnet die Terminal-Sidebar (FA-20; ist-04-vorhaben-handy.png, ist-05-terminal-handy.png).
11. Backend-Log 3111: keine „agent-event rejected"-Zeilen.

## Screenshots neben dem Mock

- skizze-01.jpg (Neue Absicht) ↔ ist-03-neu-mac.png: Textfeld, Modellwahl, Starten — drei Elemente wie in der Skizze; ist-03-neu-beitrag.png zeigt den Text als ersten Beitrag im Gespräch.
- skizze-01.jpg (Vorhaben-Seite, Skizze 3) ↔ ist-04-vorhaben-mac.png: Kennung groß, Titel, Phasen-Chips rechts; ist-04-aktionsleiste-mac.png: nächster Schritt und „Freigeben" unter dem Dokument. Abweichung: die Aktionsleiste steht am Ende des Dokuments (Scrollen nötig), nicht fest am unteren Rand — dort liegt die Sende-Leiste für Anmerkungen. **Befund nach Merge (Michael, 16.09.):** Dokument links, Gespräch rechts mit fester Breite — Skizze 3 und FA-12 verlangen Gespräch links, Dokument rechts, 50/50. Behoben im Split-Fix (unten); ist-04-vorhaben-mac.png ist seitdem der Stand danach.
- Handy ohne Skizze: Beschreibung Plan §3 gilt; ist-04-vorhaben-handy.png, ist-03-neu-handy.png.

## Messwerte

- Erste Eingabe „Neue Absicht": Start → Zustellung 10,7 s (Haiku, erste Frage); Start → Ordner-Claim 45 s (zwei Antworten).
- Chip-Synchronisation zwischen zwei Geräten: 4 ms (Broadcast-Empfang → DOM).
- Freigabe ohne Sitzung: Start → Zustellung 108,6 s (Lauf 1) bzw. 178 s (Lauf 2); Start → `Status: freigegeben` 139 s bzw. 153 s.

# E2E-Protokoll INT-2026-010 Split-Fix (nach Stufe 2) — 2026-09-16

Befund von Michael am Screenshot der Vorhaben-Seite: Dokument links (1fr), Gespräch rechts mit fester Breite `clamp(420px, 34vw, 540px)` — Bestand aus INT-2026-007, nie an Skizze 3 / FA-12 angepasst (Gespräch links, Dokument rechts, Trennlinie mittig). Fix: Grid `1fr 1fr`, Gespräch-Spalte zuerst, Gespräch fixiert am linken View-Rand mit halber Inhaltsbreite (Viewport minus Terminal-Sidebar, Dateibaum, View-Padding, Spaltenabstand), Sende-Leiste beginnt an der Dokumentspalte; unter 1024 px Dokument über Gespräch (Flex-Spalte mit `order`). Skript e2e-split.mjs im Scratchpad, Branch-Backend 3111, Scratch-Projekt INT-2026-001. 5/5 Prüfpunkte grün.

## Geometrie (Bounding-Boxen, px)

- 1440×900: Gespräch l 29 · w 680 — Dokument l 731 · w 680 — Sende-Leiste l 731 · r 1440 (ist-04-vorhaben-mac.png).
- 1440×900 + Terminal-Sidebar (Cmd+D): Gespräch l 29 · w 430 — Dokument l 481 · w 430 — Leiste r 940 = linker Rand der Sidebar (ist-04-vorhaben-terminal-mac.png).
- 900×1000: Dokument oben (w 842), Gespräch darunter statisch (w 842, 70vh), Leiste volle Breite (ist-04-vorhaben-900.png). Zwei Fehler dabei gefunden und behoben: `aos-gespraech` ist ein Inline-Element (brauchte `display: block`), `align-items: start` der Grundregel verhinderte das Strecken in der Spalte.
