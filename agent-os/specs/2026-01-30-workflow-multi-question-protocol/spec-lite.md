# Spec Lite: Workflow Multi-Question Protocol

Multi-Question-Protokoll das mehrere AskUserQuestion Tool-Calls sammelt und in einer Tab-UI präsentiert, anstatt sie zu überschreiben. Backend sammelt Fragen, Frontend zeigt Tab-Navigation, User beantwortet alle und submittet gebatched.
