# Kanban Story Execution (Lite)

Drag & Drop einer Story von Backlog → In Progress startet automatisch `/execute-tasks` für diese Story. DoR und Dependencies werden vor dem Drop validiert. Die Story zeigt einen Working Indicator während der Ausführung.
