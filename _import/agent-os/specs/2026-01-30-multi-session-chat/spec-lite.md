# Multi-Session Chat (Lite)

Multi-Session Chat ermöglicht paralleles Arbeiten mit mehreren unabhängigen Chat-Sessions. Jede Session behält Chat-Historie und Agent-State, Sessions werden persistent in `agent-os/sessions/` gespeichert und können archiviert/wiederhergestellt werden. Navigation erfolgt über eine Tab-Leiste im Browser-Tab-Stil.
