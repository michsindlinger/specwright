# no-secrets: versionierte UI-Configs ohne Zugänge (INT-2026-011).
# Eine erweiterte Regex je Zeile gegen den staged Pfad; nur die Dateinamen-Regel wird ausgenommen,
# die Inhaltsregel prüft weiter jede hinzugefügte Zeile.
^ui/config/(model-config|general-config|prompt-templates)\.json$
