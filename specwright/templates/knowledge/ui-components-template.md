# UI Components

> Verfügbare UI-Komponenten im Projekt.
> Zuletzt aktualisiert: [DATE]

## Komponenten-Übersicht

| Komponente | Pfad | Props/Variants | Erstellt in Spec |
|------------|------|----------------|------------------|
| - | - | - | - |

---

## Komponenten-Details

<!--
Für jede Komponente füge einen Abschnitt hinzu:

### [ComponentName]

**Pfad:** `src/components/[path]/[ComponentName].tsx`
**Erstellt:** [SPEC_NAME] ([DATE])

**Props:**
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| variant | 'primary' \| 'secondary' | 'primary' | Button style variant |
| size | 'sm' \| 'md' \| 'lg' | 'md' | Size of the component |

**Usage Example:**
```tsx
import { ComponentName } from '@/components/[path]';

<ComponentName variant="primary" size="md">
  Content
</ComponentName>
```

**Notes:**
- Wichtige Hinweise zur Nutzung
- Bekannte Einschränkungen
-->

---

## Design Tokens

> Dokumentiere hier Design-System-Tokens die von Komponenten verwendet werden.

<!--
Beispiel:

**Colors:**
- Primary: `--color-primary` (#3B82F6)
- Secondary: `--color-secondary` (#6B7280)

**Spacing:**
- `--space-xs` (4px)
- `--space-sm` (8px)
- `--space-md` (16px)

**Border Radius:**
- `--radius-sm` (4px)
- `--radius-md` (8px)
-->

---

## Patterns & Conventions

> Dokumentiere hier UI-Patterns die im Projekt etabliert wurden.

<!--
Beispiel:

### Form Pattern
Alle Formulare nutzen `react-hook-form` mit `zod` Validierung.

### Loading States
Komponenten mit async Daten nutzen das `<Skeleton />` Pattern.

### Error Handling
Fehler werden via `<ErrorBoundary />` gefangen und `<ErrorMessage />` angezeigt.
-->

---

*Template Version: 1.0*
