# Document Preview Panel

Overlay Side-Panel in der Specwright UI, das von Claude Code via MCP-Tools gesteuert wird, um waehrend Workflows generierte Markdown-Dokumente automatisch anzuzeigen und editierbar zu machen. Kommunikation ueber Filesystem IPC (MCP → /tmp/ JSON → Backend Filewatcher → WebSocket → Frontend).
